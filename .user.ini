; Force PHP to check for file changes on every request
; (default is 2 seconds on many hosts, but some set it much higher)
opcache.revalidate_freq=0

; Disable opcache file timestamp validation caching
opcache.validate_timestamps=1
