// Placeholder entry point for the VTT combat tracker.
// Implementation will lazily mount tracker UI modules once the panel is opened.
export function initializeCombatTracker(options = {}) {
  // TODO: wire services, state store, and UI renderers.
  return { dispose() {} };
}
